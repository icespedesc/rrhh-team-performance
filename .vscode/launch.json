{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Desktop App",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Build Shareable Output",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dist",
      "cwd": "${workspaceFolder}"
    }
  ]
}